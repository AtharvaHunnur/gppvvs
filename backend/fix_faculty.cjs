const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const faculty = await prisma.faculty.findMany(); let updated = 0; for (const f of faculty) { if (!isNaN(parseInt(f.name))) { console.log('Fixing Faculty ID:', f.id); const position = parseInt(f.name); const name = f.designation; const designation = f.specialization; const specialization = f.experience; const experience = null; await prisma.faculty.update({ where: { id: f.id }, data: { position, name, designation, specialization, experience } }); updated++; } } console.log('Updated ' + updated + ' faculty records.'); } main().catch(console.error).finally(() => prisma.$disconnect());
